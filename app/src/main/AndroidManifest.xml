<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/quizerr"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/quizerr_round"
        android:supportsRtl="true"
        android:theme="@style/Quizer">
        <activity
            android:name=".activities.CreateTestActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.HomePanelActivity" />
        <activity android:name=".activities.ResetPasswordActivity" />
        <activity android:name=".activities.AccountDetailsActivity" />
        <activity android:name=".activities.CreateCourseActivity" />
        <activity android:name=".activities.CoursesActivity" />
        <activity android:name=".activities.MyCoursesActivity" />
        <activity android:name=".activities.ResultsActivity" />
        <activity android:name=".activities.ActiveTestsActivity" />
        <activity android:name=".activities.ChooseCourseActivity" />
        <activity android:name=".activities.SolveTestActivity" />
        <activity android:name=".activities.MyTestsActivity" />
    </application>
</manifest>